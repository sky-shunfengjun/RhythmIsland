# 律动岛 · RhythmIsland

律动岛是面向 ClassIsland 的实时音频频谱背景插件。

当前 `0.1.0.0` 版本包含设置保存、默认扬声器回环捕获、频谱分析、诊断页面和“律动岛频谱”主界面组件。插件不会安装、注册或改变 ClassIsland 主题。

## 使用方式

1. 在 ClassIsland 的插件市场页面安装 `RhythmIsland.cipx`。
2. 重启 ClassIsland。
3. 打开“设置 → 插件 → 律动岛”，主动开启声音捕获。
4. 在 ClassIsland 编辑模式中添加“律动岛频谱”组件。
5. 播放声音后，检查组件、设备名称、频谱帧时间和峰值是否持续变化。

每个频谱组件都有独立设置：长度、幅度和透明度使用滑块，柱数使用选择框，无声自动收起等待时间可直接输入；也能设置方向和颜色。调整一个组件不会改变其他频谱组件。

插件默认关闭。关闭开关后会停止捕获。

## 隐私

音频只在内存中实时计算；插件不写入音频文件、不联网、不上传音频，也不保存能够还原原始音频的历史数据。

## 兼容范围

- Windows 10/11 x64
- ClassIsland 2.1.0.1
- .NET 8

作者：`sky-shunfengjun`  
许可证：`GPL-3.0-or-later`
